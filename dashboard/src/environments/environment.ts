/**
 * Base dos webhooks do n8n que servem a API do dashboard (WF4).
 * Em produção, troque pela URL pública do seu n8n.
 */
export const environment = {
  apiBase: 'http://localhost:5678/webhook',
};
